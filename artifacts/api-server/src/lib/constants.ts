/** Default weekly carbon budget (kg CO₂e) per IPCC individual target */
export const DEFAULT_WEEKLY_BUDGET_KG = 100 as const;

/** Safety cap on community aggregation queries */
export const COMMUNITY_QUERY_LIMIT = 2000 as const;
