/**
 * @workspace/scripts entry point.
 *
 * Add runnable scripts (migrations, seed data, etc.) here.
 * This file is intentionally minimal — it only exists so the
 * package has at least one compilable source file.
 */
export {};
